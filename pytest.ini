[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

markers =
    unit: fast tests
    integration: integration tests
    benchmark: performance tests

addopts = -ra