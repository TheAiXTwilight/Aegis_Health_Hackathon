# Aegis Health — Python backend dependencies
# Core runtime
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
sqlalchemy>=2.0.0
loguru>=0.7.0

# Auth (JWT + bcrypt + email validation)
bcrypt>=4.0.0
pyjwt>=2.8.0
email-validator>=2.0.0

# Uploads / async I/O
aiofiles>=24.0.0
python-multipart>=0.0.12
python-magic>=0.4.27

# HTTP client / external calls
httpx>=0.27.0

# Health / system
psutil>=6.0.0

# PDF / report generation
weasyprint>=62.0
pypdf>=5.0.0
pymupdf>=1.24.0
reportlab>=4.0.0

# Data / numerical helpers
numpy>=1.26.0
sympy>=1.13.0
pandas>=2.2.0

# Audio / image / vision
Pillow>=10.0.0
pydub>=0.25.0
soundfile>=0.12.0
faster-whisper>=1.0.0

# ML / RAG (large — install once)
torch>=2.4.0
torchvision>=0.19.0
transformers>=4.44.0
onnxruntime>=1.19.0
scikit-learn>=1.5.0

# Vector stores (choose one for RAG)
# chromadb is pinned EXACTLY (not >=) — ChromaDB's on-disk collection
# metadata format has had multiple breaking changes across versions
# (e.g. the 0.5.0/0.5.1 boundary, and again in 1.0.x — see
# github.com/chroma-core/chroma issues #2377, #4380, #4386). A client
# version that doesn't match the version that wrote data/knowledge/chroma/
# will fail with KeyError: '_type' on get_collection(). If you rebuild
# the index with a different chromadb version, update this pin to match
# and note the new version in docs/corpus_version.md.
chromadb==1.5.9
faiss-cpu>=1.8.0